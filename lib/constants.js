/**
 * The system's git folder name.
 * @type {String}
 */
export const GIT_FOLDER_NAME = '.git';

/**
 * A list of default branches to ignore, since we assume they will always exist.
 * @type {String[]}
 */
export const IGNORED_BRANCHES = ['master'];
